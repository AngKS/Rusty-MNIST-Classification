[package]
name = "my_first_rust_DL_app"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
burn = { version = "0.13.0", features = [ "train", "wgpu", "vision"] }